# 羽毛球轨迹分析数据与公式说明

本文说明本项目从视频坐标到运动学统计结果的计算方法。文档描述的是分析公式和统计口径，不是运行指令。

## 1. 符号与数据约定

| 符号 | 含义 | 单位 |
|---|---|---|
| `i` | 视频帧编号 | 帧 |
| `f` | 视频帧率 | frame/s |
| `t_i` | 第 `i` 帧时间，`i / f` | s |
| `(u_i, v_i)` | 图像像素坐标 | px |
| `(x_i, y_i)` | 展开球场坐标 | m |
| `dt` | 两个有效坐标之间的时间差 | s |
| `d_i` | 两个有效坐标之间的位移距离 | m |
| `v_i` | 帧间速度 | m/s |
| `a_i` | 离散加速度 | m/s² |

视频坐标原点在左上角，`u` 向右、`v` 向下。展开球场坐标的范围为：

```text
0 <= x <= 6.1 m
0 <= y <= 13.4 m
```

运动员坐标是脚点，默认使用检测框底边中心；羽毛球坐标是 TrackNetV3 输出的球心。

## 2. 坐标来源与有效数据

每个对象的坐标来源分为：

- `detect`：当前帧由检测器直接得到。
- `flow`：当前帧未检测到目标，由 Lucas-Kanade 光流从上一坐标补偿。
- `hold`：短时无法得到光流结果时，暂时复用上一坐标。主要用于视频渲染逻辑。
- `missing`：没有可靠坐标。

有效坐标帧是坐标存在且可用于统计的帧。`missing` 不应当被解释为静止。

有效帧率：

$$
\text{coverage}(\%)=
\frac{N_{\text{valid}}}{N_{\text{total}}}\times100
$$

其中 `N_valid` 是存在有效坐标的帧数，`N_total` 是视频总帧数。有效帧率表示数据覆盖程度，不等于检测准确率。

## 3. 球场标定与单应性变换

### 3.1 四点到标准球场

将图像中的球场四边形按以下顺序映射到真实球场矩形：

```text
图像点顺序：top_left, top_right, bottom_right, bottom_left
目标点：    (0,0), (6.1,0), (6.1,13.4), (0,13.4)
```

单应性矩阵 `H` 由四组对应点求得：

$$
\begin{bmatrix}
X_i'\\Y_i'\\W_i'
\end{bmatrix}
=
H
\begin{bmatrix}
u_i\\v_i\\1
\end{bmatrix}
$$

归一化后的球场坐标为：

$$
x_i=\frac{X_i'}{W_i'},\qquad
y_i=\frac{Y_i'}{W_i'}
$$

代码使用 `cv2.getPerspectiveTransform()` 和 `cv2.perspectiveTransform()` 实现。

### 3.2 标定点口径

项目中保留过不同版本的标定点，因此必须以实际运行命令传入的 `--court_points` 为准。

- 当前交付参考文件 `court_points_41.json` 使用：
  `352,232,613,232,719,525,244,525`。
- `export_frame_coordinates.py` 的历史默认值为：
  `291,314,690,314,951,535,48,535`。

不同标定点会改变所有米制坐标、距离、速度、区域比例和覆盖面积；不能混用不同标定版本的结果。

## 4. 帧时间、位移与速度

### 4.1 时间间隔

对帧号为 `i_1` 和 `i_2` 的两个有效坐标：

$$
dt=\frac{i_2-i_1}{f}
$$

代码使用实际帧号差，因此检测中间缺失帧时不会错误地把时间间隔当作一个帧周期。

### 4.2 位移距离

$$
\Delta x=x_{i_2}-x_{i_1}
$$

$$
\Delta y=y_{i_2}-y_{i_1}
$$

$$
d=\sqrt{(\Delta x)^2+(\Delta y)^2}
$$

### 4.3 帧间速度

$$
v=\frac{d}{dt}
$$

运动员和羽毛球的基础速度均使用这个公式。速度是单目视频经单应性变换后的二维投影速度，不是三维真实速度。

### 4.4 总距离、平均速度和最大速度

对所有通过异常过滤的帧间位移：

$$
D=\sum_{i=1}^{n}d_i
$$

$$
T=\sum_{i=1}^{n}dt_i
$$

$$
\bar v=\frac{D}{T}
$$

$$
v_{\max}=\max_i(v_i)
$$

这里的平均速度是“总距离 / 有效累计时间”，不是简单地对所有帧速度做算术平均。

运动员统计默认排除 `v_i > 8.5 m/s` 的帧间跳变。羽毛球导出脚本默认排除 `v_i > 100 m/s` 的跳变，并将对应帧标记为 `ball_jump_flag=1`。

## 5. 位置平滑与速度显示

视频渲染层对运动员球场位置使用中值滤波。若位置窗口为 `k`：

$$
\tilde x_i=\operatorname{median}(x_{i-k+1},...,x_i)
$$

$$
\tilde y_i=\operatorname{median}(y_{i-k+1},...,y_i)
$$

当前默认位置窗口是 5 帧。

显示速度还会进入速度缓冲区，并取缓冲区中非零速度的中位数：

$$
v^{display}_i=\operatorname{median}\{v_j\mid v_j>0,\ j\in\text{speed window}\}
$$

当前默认显示速度窗口是 8 帧。这个平滑值用于视频面板和加速度显示；距离累计使用通过速度过滤后的帧间距离。

## 6. 加速度与减速

### 6.1 离散加速度

相邻速度值的离散加速度为：

$$
a_i=\frac{v_i-v_{i-1}}{dt}
$$

视频逐帧处理且 `dt=1/f` 时，代码等价于：

$$
a_i=(v_i-v_{i-1})f
$$

代码中的视频加速度使用显示速度 `v^{display}`，所以它是用于可视化的离散估计，不是经过物理滤波的连续加速度。

### 6.2 平均绝对加速度

$$
\overline{|a|}=\frac{1}{N}\sum_{i=1}^{N}|a_i|
$$

平均绝对加速度越大，表示速度变化越剧烈。它会受到脚点抖动、检测误差和身份切换影响。

### 6.3 最大绝对加速度

$$
|a|_{\max}=\max_i|a_i|
$$

### 6.4 减速次数

当：

$$
a_i<-1.0\ \text{m/s}^2
$$

计为一次减速事件。该指标只适合相对比较，不应直接解释为运动员生理负荷。

## 7. 速度标准差与静止比例

速度标准差为：

$$
\sigma_v=
\sqrt{\frac{1}{N}\sum_{i=1}^{N}(v_i-\bar v)^2}
$$

其中 `v_i` 应使用通过异常速度过滤的有效速度。

静止比例的通用定义为：

$$
\text{stationary}(\%)=
\frac{N_{v\le v_{\text{stop}}}}{N_{\text{valid}}}\times100
$$

其中 `v_stop` 是预先定义的静止速度阈值。

注意：交付表保留了静止比例，但当前仓库没有统一、明确命名的 `v_stop` 参数。因此复现该列时必须同时记录所采用的静止阈值，不能仅根据最终数值反推。

## 8. 轨迹覆盖面积

将有效球场坐标点组成集合：

$$
P=\{(x_i,y_i)\mid i\text{ 为有效帧}\}
$$

对 `P` 求二维凸包 `CH(P)`，覆盖面积为：

$$
A_{coverage}=\operatorname{Area}(CH(P))
$$

单位为平方米。覆盖面积表示活动空间范围，不是运动员实际跑过的距离。

## 9. 前场、中场、后场与左右区域

### 9.1 近端运动员区域

近端球员所在半场长度为 `6.7 m`。令：

$$
r=\frac{y-6.7}{6.7}
$$

并将 `r` 限制在 `[0,1]`：

$$
r'=\operatorname{clip}(r,0,1)
$$

区域定义为：

$$
\begin{cases}
\text{front}, & r'<1/3\\
\text{mid}, & 1/3\le r'<2/3\\
\text{back}, & r'\ge2/3
\end{cases}
$$

### 9.2 区域比例

对每个区域 `z`：

$$
R_z(\%)=
\frac{N_z}{N_{front}+N_{mid}+N_{back}}\times100
$$

区域统计只计入通过质量门槛的有效运动员坐标。

### 9.3 左右半场

按球场宽度中线 `x=6.1/2=3.05 m` 划分：

$$
\text{left}:x<3.05
$$

$$
\text{right}:x\ge3.05
$$

左右比例也使用有效坐标帧数作为分母。

## 10. 羽毛球方向变化与异常跳变

### 10.1 运动方向

每个有效位移向量的方向角为：

$$
\theta_i=\operatorname{atan2}(\Delta y_i,\Delta x_i)
$$

转换为角度后，使用环绕角度差：

$$
\Delta\theta_i=
\left|((\theta_i-\theta_{i-1}+180)\bmod360)-180\right|
$$

当：

$$
\Delta\theta_i>30^\circ
$$

计为一次方向变化。

### 10.2 异常跳变

羽毛球：

$$
\text{ball\_jump\_flag}_i=
\begin{cases}
1,&v_i>100\text{ m/s}\\
0,&v_i\le100\text{ m/s}
\end{cases}
$$

运动员：

$$
\text{player jump}_i=
\begin{cases}
\text{排除},&v_i>8.5\text{ m/s}\\
\text{保留},&v_i\le8.5\text{ m/s}
\end{cases}
$$

异常坐标仍可保留在逐帧 CSV 中，但不进入过滤后的距离、平均速度和最大速度统计。

## 11. 回合识别

回合开始需要满足连续确认帧条件。每帧的活动条件为：

$$
active_i=(ball\_visible_i)
\lor
(quality_i\ge q_{rally}\land v^{activity}_i\ge v_{start})
$$

默认参数：

```text
q_rally = 0.40
v_start = 0.55 m/s
连续确认帧数 = 5
```

连续满足活动条件达到确认帧数后，开始一个候选回合。

回合结束需要同时满足：

$$
N_{ball\_missing}\ge30
$$

且：

$$
N_{quiet}\ge15
$$

其中安静帧条件为：

$$
quiet_i=(v^{activity}_i\le0.20)\lor(quality_i<0.40)
$$

当前回合识别是规则推断，不等同于人工标注的真实回合边界。

## 12. 回位时间

近端运动员回位点为：

$$
x_{home}=\frac{6.1}{2}=3.05
$$

$$
y_{home}=6.7+6.7r_{home}
$$

默认 `r_home=0.56`，因此默认回位点约为：

```text
(3.05, 10.452) m
```

当前位置到回位点的距离：

$$
d_{home}=\sqrt{(x-x_{home})^2+(y-y_{home})^2}
$$

启动回位计时的条件：

$$
d_{home}\ge1.6\text{ m}
\land
v^{activity}\ge1.2\text{ m/s}
$$

回到回位区域的条件：

$$
d_{home}\le0.9\text{ m}
\land
t_{elapsed}\ge0.2\text{ s}
$$

回位时间：

$$
t_{return}=\frac{i_{return}-i_{start}}{f}
$$

若计时超过 4 秒仍未回位，则本次计时失效并重置。回位计时是规则事件，不代表已经完成正式的击球事件标注。

## 13. 轨迹质量分数

运动员轨迹质量分数 `q` 的基础值按坐标来源设置：

$$
q_{base}=
\begin{cases}
0.82+0.18c,&\text{detect}\\
0.62,&\text{flow}\\
0.35,&\text{hold}\\
0,&\text{missing}
\end{cases}
$$

其中 `c` 是检测置信度，并限制在 `[0,1]`。

若当前点与上一点的像素距离超过 `70 px`，则进行跳变惩罚：

$$
q=0.35q_{base}
$$

最后将 `q` 限制在 `[0,1]`。统计至少要求：

```text
quality_min_for_stats = 0.45
```

回合活动判断至少要求：

```text
quality_min_for_rally = 0.40
```

## 14. 检测评价指标：需要人工真值

以下指标不能从当前自动结果独立计算，必须有逐帧人工真值坐标或身份标签。

### 14.1 坐标误差

预测坐标 `p_i` 与真值坐标 `g_i` 的欧氏误差：

$$
e_i=\|p_i-g_i\|_2
$$

平均绝对误差：

$$
MAE=\frac{1}{N}\sum_i e_i
$$

均方根误差：

$$
RMSE=\sqrt{\frac{1}{N}\sum_i e_i^2}
$$

### 14.2 PCK

给定像素容差 $\tau$：

$$
PCK(\tau)=
\frac{1}{N}\sum_i\mathbf{1}(e_i\le\tau)
$$

### 14.3 检测 Precision、Recall、F1

在指定空间容差下定义 TP、FP、FN：

$$
Precision=\frac{TP}{TP+FP}
$$

$$
Recall=\frac{TP}{TP+FN}
$$

$$
F1=\frac{2\cdot Precision\cdot Recall}{Precision+Recall}
$$

### 14.4 ADE 与 FDE

平均位移误差：

$$
ADE=\frac{1}{N}\sum_{i=1}^{N}\|p_i-g_i\|_2
$$

终点位移误差：

$$
FDE=\|p_T-g_T\|_2
$$

### 14.5 跟踪身份指标

IDF1、MOTA 和 ID Switches 需要真实身份轨迹。典型 MOTA 定义为：

$$
MOTA=1-\frac{FN+FP+IDS}{GT}
$$

其中 `GT` 是真值目标总数，`IDS` 是身份切换次数。没有人工身份真值时，模型的 `track_id` 不能当作评价真值。

## 15. 三维量的限制

当前所有米制轨迹来自单应性变换，因此只能表示球场平面上的二维投影：

$$
v_{2D}=\frac{\sqrt{(\Delta x)^2+(\Delta y)^2}}{dt}
$$

无法仅凭当前单目视频得到：

- 羽毛球真实三维速度；
- 羽毛球高度和飞行深度；
- 三维加速度；
- 可靠的空间击球点。

这些指标需要双目、多视角或深度相机，以及相机内外参数。

## 16. 复现与解释注意事项

1. 先固定视频帧率、帧号定义和球场四点，再比较不同实验结果。
2. 不同标定点产生的米制结果不能直接横向比较。
3. `detect`、`flow` 和 `hold` 的数据来源应分开记录。
4. `missing` 不是零速度，也不应计入静止比例。
5. 异常跳变过滤阈值必须和最终表格一起记录。
6. 交付表中的运动员汇总指标包含部分历史派生统计；当前 `export_frame_coordinates.py` 主要直接生成逐帧坐标和羽毛球统计，不能独立重建所有运动员汇总列。
7. 没有人工真值时，不能声称 MAE、RMSE、PCK、Precision、Recall、F1、IDF1 或 MOTA 代表真实检测性能。

## 17. 主要实现位置

- 球场变换、逐帧导出和羽毛球统计：`pipeline_code/export_frame_coordinates.py`
- 运动员运动统计、回合识别、区域比例和回位计时：`pipeline_code/overlay_player_analytics.py`
- 交付指标与限制：`docs/轨迹分析交付文档.md`
- 最终逐帧结果：`output_coordinates/final/41_coordinates_locked.csv`
